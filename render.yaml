services:
  - type: web
    name: aave-credit-score-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn app:app --chdir backend
    envVars:
      - key: FLASK_ENV
        value: production
    plan: free
